{
  "name": "460-trip-planner-native",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor wrapper project root for 460 Trip Planner iOS/Android builds.",
  "scripts": {
    "cap:sync": "npm run build --prefix client && cap sync",
    "cap:open:android": "cap open android",
    "android:debug": "npm run build --prefix client && cap copy android && cd android && ./gradlew assembleDebug"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0"
  }
}
